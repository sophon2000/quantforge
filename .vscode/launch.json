{
	"version": "0.2.0",
	"configurations": [
	  {
		"name": "Run CLI (serve)",
		"type": "go",
		"request": "launch",
		"mode": "debug",
		"program": "${workspaceFolder}/cmd/cli",
		"args": ["serve"],
		"cwd": "${workspaceFolder}",
		"env": {},
		"envFile": "${workspaceFolder}/.env"
	  }
	]
  }
